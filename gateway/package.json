{
  "name": "gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Per-member metered OpenRouter gateway (single upstream key, monthly USD budget per member)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "test:integration": "node test/integration.mjs"
  },
  "dependencies": {
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
